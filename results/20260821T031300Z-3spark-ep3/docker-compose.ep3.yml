# SPDX-License-Identifier: MIT
#
# TP=1 / DP=3 / EP=3 launch for DeepSeek-V4-Flash-0731 across three DGX Sparks.
#
# This file is a SIBLING of docker-compose.yml, never a replacement. The 2-Spark
# TP=2 deployment stays exactly as it was; `docker compose -f docker-compose.yml
# up -d` restores it with no edits. See docs/dgx spark/dsv4-restore-2spark.md.
#
# Why this shape:
#   TP=1  -> attention head divisibility stops mattering (64 % 1 == 0). TP=3 is
#            genuinely impossible: 64 heads / 4096 hidden / 256 experts are all
#            indivisible by 3.
#   DP=3  -> one engine core per node, each owning a full attention replica.
#   EP=3  -> ep_size = dp_size * pcp_size * tp_size = 3. Expert parallelism hands
#            out WHOLE experts, so 256 splits 86/85/85 with no tensor slicing.
#            That is the 94.3% of the checkpoint that actually needs sharding.
#
# INTERNAL vs EXTERNAL DP load balancing -- this cost a failed launch, so it is
# worth stating plainly:
#
#   --data-parallel-rank forces EXTERNAL LB mode. arg_utils.py:1975 reads
#       data_parallel_external_lb = self.data_parallel_external_lb
#                                   or self.data_parallel_rank is not None
#   and external LB then FORBIDS --headless (v1/engine/utils.py:1243,1297:
#   "Remote engine 1 must not use --headless in external or hybrid dp lb mode").
#   External LB means three independent API servers and your own load balancer
#   in front of them.
#
#   For ONE endpoint with vLLM balancing internally -- which is what matches the
#   2-Spark baseline and what the benchmark harness expects -- the workers use
#   --data-parallel-start-rank together with --headless, and the head passes NO
#   rank flag at all. That is what DP_START_RANK_ARG carries below: empty on the
#   head, "--data-parallel-start-rank N" on each worker.
#
# The compose project name differs from the 2-Spark stack so the two can never
# collide in Docker's state, even if someone starts both by mistake.
name: dspark-vllm-ep3

services:
  vllm-dspark-ep3:
    image: ${DSPARK_VLLM_IMAGE:-ghcr.io/anemll/dspark-vllm-gx10:0.1.1}
    pull_policy: if_not_present
    entrypoint: []
    network_mode: host
    ipc: host
    shm_size: "64gb"

    ulimits:
      memlock: -1
      stack: 67108864

    gpus: all

    devices:
      - /dev/infiniband:/dev/infiniband

    volumes:
      - ${HF_CACHE:-${HOME}/.cache/huggingface}:/cache/huggingface
      - ${DSPARK_MODEL_HOST:?set DSPARK_MODEL_HOST}:/models/dsv4-abliterated:ro
      - ${DSPARK_TMP_HOST:-/var/lib/dspark-tmp}:/tmp

    environment:
      HF_HOME: /cache/huggingface
      HF_HUB_OFFLINE: "${HF_HUB_OFFLINE:-1}"
      TRANSFORMERS_OFFLINE: "${TRANSFORMERS_OFFLINE:-1}"
      HF_HUB_DISABLE_XET: "${HF_HUB_DISABLE_XET:-1}"
      VLLM_CACHE_ROOT: /cache/huggingface/vllm-cache
      DSPARK_SLOT_CLAMP: "${DSPARK_SLOT_CLAMP:-1}"
      VLLM_HOST_IP: "${VLLM_HOST_IP:-}"

      VLLM_ALLOW_LONG_MAX_MODEL_LEN: "${VLLM_ALLOW_LONG_MAX_MODEL_LEN:-1}"
      VLLM_TRITON_MLA_SPARSE: "${VLLM_TRITON_MLA_SPARSE:-1}"
      VLLM_SPARSE_INDEXER_MAX_LOGITS_MB: "${VLLM_SPARSE_INDEXER_MAX_LOGITS_MB:-256}"
      VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS: "${VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS:-0}"
      VLLM_SKIP_INIT_MEMORY_CHECK: "${VLLM_SKIP_INIT_MEMORY_CHECK:-1}"
      VLLM_USE_FLASHINFER_SAMPLER: "${VLLM_USE_FLASHINFER_SAMPLER:-1}"

      VLLM_USE_B12X_MOE: "${VLLM_USE_B12X_MOE:-1}"
      VLLM_USE_B12X_WO_PROJECTION: "${VLLM_USE_B12X_WO_PROJECTION:-1}"
      VLLM_B12X_W4A16_FORCE_BLOCKS_PER_SM: "${VLLM_B12X_W4A16_FORCE_BLOCKS_PER_SM:-0}"
      VLLM_B12X_W4A16_FORCE_BLOCKS_MAX_M: "${VLLM_B12X_W4A16_FORCE_BLOCKS_MAX_M:-16}"
      VLLM_B12X_W4A16_FORCE_TILE_CONFIG: "${VLLM_B12X_W4A16_FORCE_TILE_CONFIG:-}"
      B12X_W4A16_TC_DECODE: "${B12X_W4A16_TC_DECODE:-0}"

      VLLM_DSPARK_CONFIDENCE_THRESHOLD: "${VLLM_DSPARK_CONFIDENCE_THRESHOLD:-0.0}"
      VLLM_DSPARK_CONFIDENCE_SCHEDULER: "${VLLM_DSPARK_CONFIDENCE_SCHEDULER:-off}"
      VLLM_DSPARK_LOCAL_ARGMAX: "${VLLM_DSPARK_LOCAL_ARGMAX:-1}"
      VLLM_DSPARK_REPLICATE_MARKOV_W1: "${VLLM_DSPARK_REPLICATE_MARKOV_W1:-1}"
      VLLM_DSPARK_FUSED_MARKOV_ARGMAX: "${VLLM_DSPARK_FUSED_MARKOV_ARGMAX:-0}"
      VLLM_DSPARK_GPU_REJECTED_CONTEXT_MASK: "${VLLM_DSPARK_GPU_REJECTED_CONTEXT_MASK:-1}"
      VLLM_DSPARK_REFERENCE_KV_QUANT_DEQUANT: "${VLLM_DSPARK_REFERENCE_KV_QUANT_DEQUANT:-0}"
      VLLM_DSPARK_HARDWARE_SCHEDULER_EARLY_STOP: "${VLLM_DSPARK_HARDWARE_SCHEDULER_EARLY_STOP:-1}"
      VLLM_DSV4_B12X_COMPRESSED_MLA: "${VLLM_DSV4_B12X_COMPRESSED_MLA:-0}"
      VLLM_DSV4_DSPARK_DEFER_TARGET_CAPTURE: "${VLLM_DSV4_DSPARK_DEFER_TARGET_CAPTURE:-0}"
      VLLM_DSV4_DSPARK_DEFER_TARGET_CAPTURE_EXACT: "${VLLM_DSV4_DSPARK_DEFER_TARGET_CAPTURE_EXACT:-0}"

      TORCH_CUDA_ARCH_LIST: "${TORCH_CUDA_ARCH_LIST:-12.1a}"
      FLASHINFER_CUDA_ARCH_LIST: "${FLASHINFER_CUDA_ARCH_LIST:-12.1a}"
      FLASHINFER_DISABLE_VERSION_CHECK: "${FLASHINFER_DISABLE_VERSION_CHECK:-1}"
      FLASHINFER_WORKSPACE_BASE: "${FLASHINFER_WORKSPACE_BASE:-/cache/huggingface/flashinfer}"
      TILELANG_CLEANUP_TEMP_FILES: "${TILELANG_CLEANUP_TEMP_FILES:-1}"
      DG_JIT_USE_NVRTC: "${DG_JIT_USE_NVRTC:-0}"
      DG_JIT_NVCC_COMPILER: "${DG_JIT_NVCC_COMPILER:-/usr/local/cuda/bin/nvcc}"
      PYTORCH_CUDA_ALLOC_CONF: "${PYTORCH_CUDA_ALLOC_CONF:-expandable_segments:True}"

      NCCL_NET: "${NCCL_NET:-IB}"
      NCCL_IB_DISABLE: "${NCCL_IB_DISABLE:-0}"
      NCCL_IB_HCA: "${NCCL_IB_HCA}"
      NCCL_SOCKET_IFNAME: "${NCCL_SOCKET_IFNAME}"
      TP_SOCKET_IFNAME: "${TP_SOCKET_IFNAME:-enp1s0f0np0}"
      GLOO_SOCKET_IFNAME: "${GLOO_SOCKET_IFNAME:-enp1s0f0np0}"
      NCCL_IB_GID_INDEX: "${NCCL_IB_GID_INDEX:-}"
      NCCL_IB_ADDR_FAMILY: "${NCCL_IB_ADDR_FAMILY:-AF_INET}"
      NCCL_IB_ROCE_VERSION_NUM: "${NCCL_IB_ROCE_VERSION_NUM:-2}"
      NCCL_IB_ADDR_RANGE: "${NCCL_IB_ADDR_RANGE:-}"
      NCCL_CROSS_NIC: "${NCCL_CROSS_NIC:-1}"
      NCCL_CUMEM_ENABLE: "${NCCL_CUMEM_ENABLE:-0}"
      NCCL_IGNORE_CPU_AFFINITY: "${NCCL_IGNORE_CPU_AFFINITY:-1}"
      NCCL_DEBUG: "${NCCL_DEBUG:-WARN}"
      NCCL_NVLS_ENABLE: "${NCCL_NVLS_ENABLE:-0}"

      # The 2-Spark stack pins --moe-backend flashinfer_b12x. That kernel does NOT
      # implement expert parallelism and hard-fails at model init:
      #   ValueError: Mxfp4 MoE backend 'B12X_MXFP4' does not support the deployment
      #   configuration ... FusedMoEParallelConfig(tp_size=1, dp_size=3, ep_size=3,
      #   use_ep=True)
      # "auto" lets vLLM pick a kernel that supports the actual parallel config.
      # Keep B12X off for the same reason.
      MOE_BACKEND: "${MOE_BACKEND:-auto}"
      DP_START_RANK_ARG: "${DP_START_RANK_ARG:-}"
      DP_ADDRESS: "${DP_ADDRESS}"
      DP_RPC_PORT: "${DP_RPC_PORT:-25100}"
      HEADLESS: "${HEADLESS:-}"
      MTP_NUM_TOKENS: "${MTP_NUM_TOKENS:-5}"

    command:
      - bash
      - -lc
      - >
        export PATH="/usr/local/cuda/bin:$${PATH:-}";
        export CUDA_HOME="$${CUDA_HOME:-/usr/local/cuda}";
        export CUDA_PATH="$${CUDA_PATH:-$${CUDA_HOME}}";
        export CUDAToolkit_ROOT="$${CUDAToolkit_ROOT:-$${CUDA_HOME}}";
        export LD_LIBRARY_PATH="/usr/local/cuda/lib64:$${LD_LIBRARY_PATH:-}";
        SPECULATIVE_CONFIG="{\"method\":\"dspark\",\"num_speculative_tokens\":${MTP_NUM_TOKENS:-5},\"draft_sample_method\":\"probabilistic\"}";
        exec /usr/local/bin/vllm serve ${DSPARK_MODEL:-/models/dsv4-abliterated}
        --served-model-name ${SERVED_MODEL_NAME:-deepseek-v4-flash-0731}
        --host ${VLLM_HOST:-127.0.0.1}
        --port ${VLLM_PORT:-8100}
        --trust-remote-code
        --tensor-parallel-size 1
        --pipeline-parallel-size 1
        --data-parallel-size 3
        --data-parallel-size-local 1
        ${DP_START_RANK_ARG}
        --data-parallel-address ${DP_ADDRESS}
        --data-parallel-rpc-port ${DP_RPC_PORT:-25100}
        --enable-expert-parallel
        --kv-cache-dtype nvfp4_ds_mla
        --block-size 256
        --max-model-len ${MAX_MODEL_LEN:-460800}
        --max-num-seqs ${MAX_NUM_SEQS:-16}
        --max-num-batched-tokens ${MAX_NUM_BATCHED_TOKENS:-8192}
        --max-cudagraph-capture-size $$(( ${MAX_NUM_SEQS:-16} * (${MTP_NUM_TOKENS:-5} + 1) ))
        --gpu-memory-utilization ${GPU_MEMORY_UTILIZATION:-0.85}
        --enable-prefix-caching
        --async-scheduling
        --enable-chunked-prefill
        --speculative-config "$${SPECULATIVE_CONFIG}"
        --tokenizer-mode deepseek_v4
        --distributed-executor-backend mp
        --moe-backend ${MOE_BACKEND:-auto}
        --tool-call-parser deepseek_v4
        --enable-auto-tool-choice
        --reasoning-parser deepseek_v4
        --reasoning-config '{"reasoning_parser":"deepseek_v4","reasoning_start_str":"<think>","reasoning_end_str":"</think>"}'
        --default-chat-template-kwargs '{"thinking":false}'
        --generation-config vllm
        --enable-flashinfer-autotune
        --jit-monitor-mode ${JIT_MONITOR_MODE:-warn}
        ${JIT_MONITOR_VERBOSE:+--jit-monitor-verbose}
        ${HEADLESS:+--headless}
